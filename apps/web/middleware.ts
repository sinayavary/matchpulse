import { NextResponse, type NextRequest } from "next/server";
export function middleware(request: NextRequest) { const path = request.nextUrl.pathname; if (path.startsWith("/api/internal")) return new NextResponse("Not found", { status: 404 }); if (["POST", "PUT", "PATCH", "DELETE"].includes(request.method)) { const origin = request.headers.get("origin"); const referer = request.headers.get("referer"); const expected = process.env.WEB_ORIGIN; if (!expected || (origin !== expected && !(referer?.startsWith(`${expected}/`)))) return NextResponse.json({ error: "request_rejected", message: "The request could not be completed." }, { status: 403 }); if (!request.headers.get("x-csrf-token")) return NextResponse.json({ error: "request_rejected", message: "The request could not be completed." }, { status: 403 }); } return NextResponse.next(); }
export const config = { matcher: ["/api/:path*"] };
