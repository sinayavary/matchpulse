{
  "name": "@matchpulse/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'worker lint placeholder'",
    "test": "tsx --test src/worker-config.test.ts src/worker-runner.test.ts"
  },
  "dependencies": {
    "@matchpulse/shared": "workspace:*",
    "@matchpulse/txline-client": "workspace:*",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
