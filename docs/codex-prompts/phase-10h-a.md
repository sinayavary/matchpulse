# Codex prompt — Phase 10H-A

Use only after this exact review-only pack is merged, reviewed, and activated by repository governance.

```text
Work directly inside D:\money\matchpulse_repo. Read AGENTS.md and docs/codex-master-plan/CODEX_ENTRYPOINT.md. Run Automation v2 Validate and execute only the repository-selected Phase 10H-A pack. Copy the committed payload exactly. Do not invent or expose private model coefficients, specialist formulas, provider weights, confidence formulas, or thresholds. Run every declared validation command, update only permitted completion metadata, then run Automation v2 Prepare. Stop with PHASE_COMPLETE_PREPARED before Publish. Do not activate 10G, 10H-B, 10H-C, 10I, or another successor.
```

Review invariants:

- no hard-coded proprietary model weights
- specialist weights are runtime inputs and must sum to one
- market weight is capped by the odds-intelligence reference
- no persistence, route, worker, frontend, migration, or network access
- no public API changes
- no successor activation
