# Codex prompt — Phase 10H-A

Use only after the exact repository-controlled 10H-A pack has been reviewed and activated.

```text
Work directly inside D:\money\matchpulse_repo. Read AGENTS.md and docs/codex-master-plan/CODEX_ENTRYPOINT.md. Run Automation v2 Validate and execute only the repository-selected Phase 10H-A pack. Apply the reviewed payload exactly; do not redesign feature coverage, market sanitization, specialist behavior, coefficients, contracts, or safety boundaries. Run every declared validation command, update only permitted completion metadata, then run Automation v2 Prepare. Stop with PHASE_COMPLETE_PREPARED before Publish. Do not activate 10H-B or another successor.
```

Review invariants:

- pure deterministic code only
- unusable odds expose zero weight and no model probability
- provider identities/raw observations never enter the feature snapshot
- all probability outputs are finite, bounded, normalized, and deterministic
- no database, route, worker, frontend, migration, network, secret, or lockfile change
- no wagering recommendation or financial language
