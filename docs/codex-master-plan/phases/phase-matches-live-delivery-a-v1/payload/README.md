# Governance payload

The source implementation is reconstructed after this governance pack is published. Future execution targets are exactly the manifest allowlist; governance files and the phase pack remain separate from implementation targets.
