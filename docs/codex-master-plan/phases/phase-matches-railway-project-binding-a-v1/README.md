# MATCHES-RAILWAY-PROJECT-BINDING-A v1

Target project `e8540514-d2b9-4585-8d2a-a62fc3c87829`, environment `staging`.

1. `PROJECT-BINDING-DIAGNOSE-READONLY`: use an explicit project identifier to compare CLI binding and project metadata, without mutation.
2. `STAGING-ENVIRONMENT-CREATE-EXPLICIT`: only after separate approval, create `staging` through an explicitly project-scoped operation, once.

No other Railway action is allowed. Evidence only: `docs/operations/matches-railway-topology-evidence.md`.
