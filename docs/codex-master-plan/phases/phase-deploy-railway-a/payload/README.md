# DEPLOY-RAILWAY-A payload

This pack intentionally carries its executable specification in the tracked manifest and phase README. No source payload is copied because implementation begins from the exact baseline files named in `allowed_target_files`.
