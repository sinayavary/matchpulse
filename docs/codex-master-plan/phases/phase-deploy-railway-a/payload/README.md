# DEPLOY-RAILWAY-A v2 payload

This pack intentionally carries its executable specification in the tracked manifest and phase README. No source payload is copied because implementation begins from the exact baseline files named in `allowed_target_files`. Version 2 adds only deterministic Next.js-generated `apps/web/next-env.d.ts` and `apps/web/tsconfig.json` to that allowlist.
