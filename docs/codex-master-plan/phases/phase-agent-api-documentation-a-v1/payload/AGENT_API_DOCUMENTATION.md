# Agent API delivery contract

The phase proves persisted fixture, state, odds and events through the enabled Agent worker into public intelligence-card and product-intelligence endpoints. Public output must not expose raw, private or internal fields. Internal routes remain fail-closed.
