# MATCHES-RAILWAY-TOPOLOGY-RETRY-B v1

Project `e8540514-d2b9-4585-8d2a-a62fc3c87829`; target `staging`; local credential `RAILWAY_TOKEN` only.

1. `STAGING-ENVIRONMENT-AUTH-PREFLIGHT-READONLY`: validate CLI/auth metadata read-only.
2. `STAGING-ENVIRONMENT-CREATE-ATTEMPT-3`: one separately approved create attempt for absent `staging`, recording exit code and sanitized stderr.

No other Railway operation is allowed. Evidence: `docs/operations/matches-railway-topology-evidence.md`.
